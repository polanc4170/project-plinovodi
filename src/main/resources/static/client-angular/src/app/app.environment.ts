const serverProtocol : string = 'http';
const serverDomain   : string = 'localhost';
const serverPort     : string = '8080';

export const serverURL : string = `${serverProtocol}://${serverDomain}:${serverPort}`;
